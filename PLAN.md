# TODO

## 当前状态

- [x] 第一版主体功能已落地：邀请注册、登录会话、空间和成员管理、相册管理、上传、预览、多选操作、回收站、管理后台、PWA 基础配置。
- [x] 历史执行流水账已归档到 `docs/AGENTS_LOG.md`。
- [x] `MEMORY.md` 已精简为长期约定、产品决策、架构约定和关键踩坑记录。
- [x] 私有媒体读取已切换为稳定应用内 URL `/api/media/[mediaId]`；相册网格和封面使用缩略图变体，预览和下载使用原始媒体，并统一使用协商缓存。
- [x] 预览主区域已限制为当前项和前后 2 项挂载原图/原视频，窗口外使用占位，避免打开预览时全量加载原图。
- [x] 缩略图和视频封面已切换为运行时数据万象 CI 方案：前端稳定 URL 为 `/api/media/[mediaId]/preview`，Next 只做权限校验、协商缓存和流式转发，不再在应用进程内压缩图片。
- [x] 腾讯云媒体处理 bucket 开通后，视频封面 `/api/media/[mediaId]/preview` 已验证可返回 CI 截帧 JPEG。
- [x] 已修复旧 `localStorage` 页面缓存导致相册页崩溃的问题：`useServerAction` 页面缓存改为 value 内 schema 版本，读取前会清理旧裸缓存和旧 key 版本缓存；媒体缩略图对缺失 `src` 做占位兜底，相册和回收站网格在 `thumbnailUrl` 缺失时回退到 `url`。
- [x] 已优化预览大图加载体验：当前原图使用高优先级加载，邻近预挂载原图降为低优先级；图片加载完成前显示沉浸式加载动画。
- [x] 已按 `.env.local` 的键顺序、注释和空行格式生成根目录 `.env.example`，并抹去数据库、密钥、存储桶、地域和管理员密码等隐私信息；可选默认项 `COS_UPLOAD_PREFIX` 保留键但值留空。
- [ ] 按用户当轮需求继续迭代；只记录当前待办、风险和验证结果。

## 持续风险

- [ ] 私有 COS 上传、读取和权限隔离。
  - 签发上传/读取授权前必须校验空间成员。
  - COS object key 禁止前导 `/`。
  - 真实环境验证非成员访问拒绝、稳定媒体路由协商缓存 304/Range/权限失效、跨空间传参拒绝、大文件分片上传。
- [ ] 媒体密集页面性能。
  - 相册缩略图保持懒加载。
  - 相册网格和封面必须使用 `thumbnailUrl`，不能直接显示原图 URL。
  - 页面数据缓存需要有版本或迁移/校验逻辑；媒体列表字段契约变化时，旧缓存不能直接进入渲染。
  - `thumbnailUrl` 指向 `/api/media/[mediaId]/preview`，图片缩略图和视频封面由数据万象 CI 运行时生成。
  - 预览主区域只挂载当前项和前后 2 项原图/原视频。
  - 刷新数据时保持媒体对象和可用 URL 稳定。
  - 预览大图保持原图，不恢复已验证有副作用的延迟替换方案。
  - 真实数据验证首次打开、快速滚动、预览连续切换的卡顿和掉帧。
- [ ] 删除、恢复和封面一致性。
  - 批量删除使用事务和批量更新。
  - 删除包含当前封面时，同一事务内切换到最新可用媒体或清空封面。
  - 普通删除和永久删除都不删除 COS 文件。
- [ ] 客户端壳和移动端导航手势。
  - 页面只渲染客户端壳，业务数据通过 Server Action 拉取。
  - 固定返回、多选返回、预览返回、上传离开保护和下拉刷新不能互相干扰。

## 每次改动检查

- [ ] 遵守用户当轮需求，不擅自扩大范围。
- [ ] 如果看到文件内容和上次修改后不一致，优先视为用户手动修改。
- [ ] 涉及 UI 时同步 `docs/design/index.html`。
- [ ] 涉及 UI 时检查移动端真实视口、安全区域、滚动区域和按钮对齐。
- [ ] 不新增 `vh`、`svh`、`h-screen`、`min-h-screen` 等传统视口高度写法。
- [ ] 根据改动范围运行 `pnpm.cmd typecheck`。
- [ ] 涉及构建、路由模式或 PWA 配置时运行 `pnpm.cmd build`。
- [ ] 涉及数据库结构时生成并验证 migration。
- [ ] 涉及媒体、上传、删除、权限时用真实数据库和真实 COS 行为验证。
